In this directory:
- py2_test_grammar.py -- test fichier that exercises most/all of Python 2.x's grammar.
- py3_test_grammar.py -- test fichier that exercises most/all of Python 3.x's grammar.
- infinite_recursion.py -- test fichier that causes lib2to3's faster recursive pattern matching
  scheme to fail, but passes when lib2to3 falls back to iterative pattern matching.
- fixes/ -- for use by test_refactor.py
